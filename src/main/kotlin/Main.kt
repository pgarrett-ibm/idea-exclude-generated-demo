// `Find in Files` for "ktlint" should find this file and not generated files in build output.
// Instead, it finds 100+ results in buildSrc/build (which is excluded) and these usually
// obscure the results that I was actually looking for, which can only be found by selecting
// the `Open In Find Window` option.
